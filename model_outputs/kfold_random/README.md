# Random stratified K-fold (archived)

Hybrid CNN trained with **image-level** frozen splits from `data/training_features/kfold_splits/` (Code 3).

These checkpoints are kept for **comparison** with spatial block CV (§2 in Code 4). Do not re-train unless you intentionally refresh random-split baselines.
